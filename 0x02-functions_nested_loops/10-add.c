#include "main.h"

/**
 *add -adds teo int and return result
 *
 *@num1:must be a number
 *@num2:must be a number
 * Return:  Always 0 (success)
 */
int add(int num1, int num2)
{
int sum;

sum = num1 + num2;
return (sum);
}
