#include <stdio.h>

/**
* main - program execution starts;it prints the strings
*
* Return: Always 0 (Success)
*/
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
