#include "main.h"

/**
*mul - multiplies two intergers
*
*@a:  argument must be an int
*
*@b:  argument must be an int
*
*Return: Always 0 (success)
*/
int mul(int a, int b)
{
int product = (a * b);
return (product);

return (0);
}
