#include "main.h"

/**
*_isupper - checks if alphabets is upper
*
*@c:  argument must be an int
*
*Return: Always 0 (success)
*/
int _isupper(int c)
{
if (c >= 65 && c <= 90)
return (1);
else
return (0);

return (0);
}
