#include "main.h"

/**
*_isdigit - checks if number is 0-9
*
*@c:  argument must be an int
*
*Return: Always 0 (success)
*/
int _isdigit(int c)
{
if (c >= 48 && c <= 57)
return (1);
else
return (0);

return (0);
}
