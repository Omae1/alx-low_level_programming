#include "main.h"

/**
 *_abs -prints the absolete number
 *
 *@x: must be a number
 * Return:  Always 0 (success)
 */
int _abs(int x)
{
if (x  >= 0)
{
return (x);
}
else
{
return (x * -1);
}
return (0);
}
